<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SimpleDynamoActivity" >

    <TextView
        android:id="@+id/textView1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/clearBtn"
        android:fadeScrollbars="true"
        android:gravity="bottom"
        android:maxLines="13"
        android:scrollbars="vertical"
        android:textIsSelectable="true" />

    <Button
        android:id="@+id/insertBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:text="Insert" />
    
    <Button
        android:id="@+id/ldumpBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/insertBtn"
        android:layout_alignTop="@id/insertBtn"
        android:layout_marginLeft="17dp"
        android:layout_toRightOf="@+id/insertBtn"
        android:text="LDump" />

    <Button
        android:id="@+id/ldeleteBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/insertBtn"
        android:layout_alignTop="@id/insertBtn"
        android:layout_marginLeft="17dp"
        android:layout_toRightOf="@+id/ldumpBtn"
        android:text="LDelete" />
    
    <Button
        android:id="@+id/clearBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/insertBtn"
        android:layout_marginTop="16dp"
        android:layout_alignLeft="@id/insertBtn"
        android:text="Clear" />
    
    <Button
        android:id="@+id/gdumpBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/clearBtn"
        android:layout_alignLeft="@id/ldumpBtn"
        android:layout_toRightOf="@id/clearBtn"
        android:text="GDump" />
    
    <Button
        android:id="@+id/gdeleteBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/clearBtn"
        android:layout_alignLeft="@id/ldeleteBtn"
        android:layout_toRightOf="@id/clearBtn"
        android:text="GDelete" />

</RelativeLayout>